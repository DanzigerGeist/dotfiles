[user]
    name  = Aleksy Rudy
    email = DanzigerGeist@icloud.com
	signingkey = C106D2DF3D33ECE6
     
[init]
    defaultBranch = master

[commit]
	gpgsign = true

[gpg]
	format = openpgp
	program = gpg

[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f

[includeIf "hasconfig:remote.*.url:git@github.Kaplan:*/**"]
    path = ~/.gitconfig-Kaplan
