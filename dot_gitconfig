[user]
    name  = Aleksy Rudy
    email = DanzigerGeist@icloud.com
	signingkey = 220D511CF2383D60 
     
[init]
    defaultBranch = master

[commit]
	gpgsign = true

[gpg]
	format = openpgp
	program = gpg

[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f

[includeIf "hasconfig:remote.*.url:git@github.Kaplan:*/**"]
    path = ~/.gitconfig-Kaplan
